package model;

public class UserAccount extends Account{

    public UserAccount(String firstName, String lastName, String username, String phoneNumber, String password) {
        super(firstName, lastName, username, phoneNumber, password);
    }
}
